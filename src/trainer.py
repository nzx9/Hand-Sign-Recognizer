class ASL_RGB_Trainer:
    def __init__(self, batch_size, learning_rate):
        self.batch_size = batch_size
        self.learning_rate = learning_rate


def train(net, dataloader, loss_func, optimizer, epochs):
    pass
